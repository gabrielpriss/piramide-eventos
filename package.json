{
  "name": "piramide-eventos",
  "private": true,
  "description": "Landing page Espaço Pirâmide Eventos — Cloudflare Pages",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "bash scripts/build-dark.sh",
    "build:images": "python3 scripts/optimize-images.py",
    "dev": "npm run build && wrangler pages dev dist --port 8788",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=piramide-eventos --branch=main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=piramide-eventos"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
